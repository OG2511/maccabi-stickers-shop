export enum OrderStatus {
  PENDING = "pending",
  CONFIRMED = "confirmed",
  REJECTED = "rejected",
}

export enum PaymentMethod {
  PAYPAL = "paypal",
  BIT = "bit",
  PAYBOX = "paybox",
}

export enum DeliveryOption {
  PICKUP = "pickup",
  ISRAEL_POST = "israel_post",
}
